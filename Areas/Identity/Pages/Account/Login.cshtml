@page
@model LoginModel

@{
    Layout = "~/Views/Shared/_Index.cshtml";
    ViewData["Title"] = "Log in";
}


<!-- <div class="row"> -->
    <!-- <div class="col-md-4"> -->
  <div class="container" style="background-color:plum;background-size:cover">
    <!--  <section> -->
      <form id="account" method="post" >
                <!--<h2>Use a local account to log in.</h2> -->
                <!-- <hr /> -->
       <!-- <h1>Budget Portal Login</h1> --> 
        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="row form-floating mb-3">
                    <div class="col"> 
                        <label asp-for="Input.Email" class="form-label">Email</label> 
                    </div>
                    <div class="col">
                     <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" style="width:fit-content" placeholder="name@example.com" />
                    </div>
                    <div class="col">
                      <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
               </div><br />
               <div class="row form-floating mb-3">
                    <div class="col">
                       <label asp-for="Input.Password" class="form-label">Password</label>
                    </div>
                    <div class="col">
                       <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" style="width:fit-content" placeholder="password" />
                    </div class="col">
                    <div class="col">
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                </div><br />
                <!--  <div class="checkbox mb-3">
                    <label asp-for="Input.RememberMe" class="form-label">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                    </label>
                </div> -->
                <br />
                <div class="row">
                    <button id="login-submit" type="submit" style="width:fit-content" class="w-20 btn btn-lg btn-primary">Log in</button>
                </div><br />
              <!--  <div>
                    <p>
                        <a id="forgot-password" asp-page="./ForgotPassword">Forgot your password?</a>
                    </p>
                    <p>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register as a new user</a>
                    </p>
                    <p>
                        <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Resend email confirmation</a>
                    </p>
                  </div> -->
                <br />
            </form>
    <!--   </section> -->
    <!--    </div> -->
    <!-- </div>  -->
    <!--  <div class="col-md-6 col-md-offset-2">
        <section>
            <h3>Use another service to log in.</h3>
            <hr />
            @*{
                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                {
                    <div>
                        <p>
                            There are no external authentication services configured. See this <a href="https://go.microsoft.com/fwlink/?LinkID=532715">article
                            about setting up this ASP.NET application to support logging in via external services</a>.
                        </p>
                    </div>
                }
                else
                {
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                        <div>
                            <p>
                                @foreach (var provider in Model.ExternalLogins!)
                                {
                                    <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                }
                            </p>
                        </div>
                    </form>
                }
            }*@
        </section>
        </div>  -->
 </div> 
<style>
    .container {
        position: relative;
        height: 100vh;
    }

    form {
        position: absolute;
        top: 25%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    #navtitle :after {
        content:'Budget Consolidation Portal Login';
    }

    #loginlink {
        display : none;
    }
</style>
@*section Scripts {
    <partial name="_ValidationScriptsPartial" />
}*@
